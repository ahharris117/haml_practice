%h1= @round.course
= @round.time
%br

%ul{:class => "users"}
  - @round.users.each do |user|
    %li= user.username
- if current_user.nil?
  %p sign up to join this round
- elsif !@round.users.include?(current_user) && @round.users.length < 4
  %button{class: "button"} Sign up
